{% set currentRoute = app.request.attributes.get('_route') %}
    <!-- Navigation -->
    <nav id="menu" class="navbar navbar-default fixed-top">
        <div class="container intro-animated hidden after" style="display: none!important;">
            <div class="navbar-header page-scroll">
                <a id="logo" class="navbar-brand page-scroll" href="{{ path('app_default_index') }}">MyWatch</a>
            </div>
            <ul class="nav">
                <li class="nav-item"><a class="nav-link active" href="{{ path('app_default_index') }}">Les montres</a></li>

                {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item"><a class="nav-link disabled" href="{{ path('app_admin_index') }}">Administration</a></li>
                    {% endif %}
                {% endif %}
                {% if app.user %}<li class="nav-item"><a class="nav-link" href="{{ path('app_default_index') }}">Mon compte</a></li>{% endif %}
                {% if app.user %}
                    <li class="nav-item btn-connexion"><a class="nav-link disabled" href="{{ path('app_security_logout') }}">Déconnexion</a></li>
                {% else %}
                    <li class="nav-item btn-connexion"><a class="nav-link disabled" href="{{ path('app_security_login') }}">Connexion</a></li>
                    <li class="nav-item btn-connexion"><a class="nav-link disabled" href="{{ path('app_security_login') }}">Inscription</a></li>
                {% endif %}

            </ul>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-fluid -->
    </nav>
