{% extends 'base.html.twig' %}

{% block body %}
    {{ parent() }}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>Formulaire d'une montre</h1>
                <hr>
            </div>
            <div class="col-md-12">
                {% form_theme montre_form 'bootstrap_3_layout.html.twig' %}
                {{ form_start(montre_form) }}
                {{ form_widget(montre_form) }}
                <button class="btn btn-info" type="submit">Ajouter</button>
                {{ form_end(montre_form) }}
            </div>
        </div>
    </div>
{% endblock %}