{% extends 'base.html.twig' %}

{% block body %}
    {{ parent() }}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>Formulaire d'une variante</h1>
                <hr>
            </div>
            <div class="col-md-12">
                {% form_theme variante_form 'bootstrap_3_layout.html.twig' %}
                {{ form_start(variante_form) }}
                {{ form_widget(variante_form) }}
                <button class="btn btn-info" type="submit">Ajouter</button>
                {{ form_end(variante_form) }}
            </div>
        </div>
    </div>
{% endblock %}